Have the player function "scan();" write to files.
	One file containing a table of all scanned IP addresses.
	Another holding all player information.

Make the dataToSend and clientToRecieve into an array - server/server.js.

Have the server check if the clientID generated by the client is associated with it's entry in the connectedClients array.
	If not, associate it.